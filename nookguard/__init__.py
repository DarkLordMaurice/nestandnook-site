"""NookGuard — Claude-only media reliability control plane for Nest & Nook.

See docs/nookguard/SPEC.md and docs/nookguard/NookGuard-Plan.docx for the full
design. This package is built incrementally per docs/nookguard/BUILD-LOG.md's
commit order; do not assume a module exists just because it's named in the spec —
check BUILD-LOG.md for what has actually landed.
"""

__version__ = "0.1.0"
