# Copy to .env for local dev only. Leave unset in production -- the
# default ('/api', same-origin) is the recommended deployment topology.
# See README.md "Deployment topology" for why.
#
# PUBLIC_NOOKGUARD_WORKER_URL=http://localhost:8787
