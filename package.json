{
  "name": "nestandnook-site",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "audit:readiness": "node scripts/site-readiness-audit.mjs",
    "audit:readiness:strict": "node scripts/site-readiness-audit.mjs --strict",
    "build": "npm run audit:readiness && astro build && pagefind --site dist --output-subdir pagefind",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "pagefind": "^1.5.2"
  }
}
